#:kivy 1.9.1

<GameSetup>:
    BoxLayout:
        size_hint: (.3, .5)
        center: root.center
        Button:
            text: "Back"
            on_press: root.go_back()
        Button:
            text: "Start"
            on_press: root.start_game()
