#:kivy 1.9.1

<Menu>:
    size_hint: (.3, .5)
    center: root.center
    Button:
        text: "Setup Game"
        on_press: root.change_to_game_setup()
    Button:
        text: "Quit"
        on_press: app.stop()
